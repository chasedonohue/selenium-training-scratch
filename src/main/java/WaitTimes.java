import java.time.Duration;

public class WaitTimes {
    public final Duration shortWait = Duration.ofSeconds(5);
    public final Duration defaultWait = Duration.ofSeconds(20);
    public final Duration longWait = Duration.ofSeconds(600);
}
