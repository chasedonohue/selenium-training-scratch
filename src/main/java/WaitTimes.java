import java.time.Duration;

public class WaitTimes {
    public final Duration SHORT_WAIT = Duration.ofSeconds(5);
    public final Duration DEFAULT_WAIT = Duration.ofSeconds(20);
    public final Duration LONG_WAIT = Duration.ofSeconds(600);
}
